# Contributing

Create a focused branch, add tests with every behavior change, run `make test`, `make lint`, `make evaluate`, and the frontend build, then explain security and migration implications in the pull request. New analytical operations require a typed schema, semantic validator, bounded executor, tests, and documentation. Never add real customer data, secrets, unrestricted generated-code execution, or SQL validation bypasses.

